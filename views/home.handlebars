<h2 class="title">Home</h2>
<h3 class="section-title">My Blog Posts</h3>
{{#if blogPosts.length}}
    <ul class="post-list">
        {{#each blogPosts}}
            <li class="post-item">
                <a href="post/{{this.id}}" class="post-link">{{this.title}}</a>
            </li>
        {{/each}}
    </ul>
{{else}}
    <p>No blog posts yet.</p>
{{/if}}
<button id="create-post-button" class="create-post-button">Create New Post</button>

<form id="create-post-form" class="create-post-form" style="display: none;" action="/post" method="POST">
    <label for="post-title">Title:</label>
    <input type="text" id="post-title" name="title" required><br>
    <label for="post-content">Content:</label><br>
    <textarea id="post-content" name="content" required></textarea><br>
    <button type="submit">Submit</button>
</form>

<script>
    const createPostButton = document.getElementById('create-post-button');
    const createPostForm = document.getElementById('create-post-form');
    
    createPostButton.addEventListener('click', function() {
        if (createPostForm.style.display === 'none') {
            createPostForm.style.display = 'block';
            createPostButton.textContent = 'Cancel New Post';
        } else {
            createPostForm.style.display = 'none';
            createPostButton.textContent = 'Create New Post';
        }
    });
</script>



